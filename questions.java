import java.util.*;


public class questions {
    public static void main(String args[]) {
        Scanner scn = new Scanner(System.in);
        // int A = scn.nextInt();
        // int B = scn.nextInt();
        // int C = scn.nextInt();

        // int average = (A + B + C)/2;
        // System.out.println(average);

        // int side = scn.nextInt();
        // int area = (side * side);
        // System.out.println(area);

        float pencil = 10.55f;
        float pen = 25.0f;
        float eraser = 5.5f;

        float  pamount = (pencil + pen + eraser);
        float bill = pamount +  (pamount * 0.18f) ;
        System.out.println(bill);






    }
}
